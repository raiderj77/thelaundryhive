# Laundry Hive Mobile App

This is the mobile application for Laundry Hive drivers and customers.

## Tech Stack
- **Framework**: React Native (via Expo)
- **Language**: TypeScript
- **Navigation**: React Navigation (To be added)
- **State**: React Context / Zustand (To be added)

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the development server:
   ```bash
   npx expo start
   ```

3. Run on device:
   - Scan the QR code with Expo Go (Android) or Camera (iOS).
   - Press `a` for Android Emulator.
   - Press `i` for iOS Simulator.

## Project Structure
- `App.tsx`: Entry point
- `assets/`: Images and fonts
- `components/`: Reusable UI components (To be created)
- `screens/`: Application screens (To be created)
